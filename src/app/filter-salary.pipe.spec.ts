import { FilterSalaryPipe } from './filter-salary.pipe';

describe('FilterSalaryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSalaryPipe();
    expect(pipe).toBeTruthy();
  });
});
